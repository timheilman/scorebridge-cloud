overwrite: true
schema:
  - schema.api.graphql
  - aws-scalars.graphql
  - aws-directives.graphql
config:
  scalars:
    AWSJSON: string
    AWSDate: string
    AWSTime: string
    AWSDateTime: string
    AWSTimestamp: number
    AWSEmail: string
    AWSURL: string
    AWSPhone: string
    AWSIPAddress: string

generates:
  appsync.d.ts:
    plugins:
      - typescript
